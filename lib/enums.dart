enum PropertyTypes {
  Apartments,
  Bungalow,
  Maisonette,
  Mansion,
  Office_blocks,
  Warehouse,
  Factory
}

enum PropertyUse { Residential, Commercial, Agricultural, Industrial }

enum BusinessType { Rent, Sale }

enum Status { Available, Taken }
